import * as React from "react";
import Button from "../Button";

export default (
  <Button
    uxpId="action1"
    size="small"
    variant="outlined"
    color="primary"
    fullWidth>
    Sign Up
  </Button>
);
